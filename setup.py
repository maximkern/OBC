from setuptools import find_packages, setup

setup (
    name = 'OBC',
    version = '0.0.1',
    packages = find_packages(),
    url = 'https://github.com/Harvard-Satellite-Team-Ground-Station',
    python_requires = '>=3.8',
    install_requires = []
)